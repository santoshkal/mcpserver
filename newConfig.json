{
  "mcpServers": {
    "GolangServer": {
      "command": "docker",
      "args": [
        "run",
        "-i",
        "--rm",
        "--mount",
        "type=bind,src=/tmp/my-test-repo,dst=/tmp/my-test-repo",
        "santoshkal/mcpserver:test"
      ]
    }
  }
}
